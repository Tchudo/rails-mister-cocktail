<div class="container">
<h1><%= @cocktail.name %></h1>
<%= link_to "<< Back to Cocktail list", cocktails_path %>

<%= link_to "+ Add more ingredients", new_dose_path(@cocktail) %>

<h2>Ingredients</h2>
<ul>

      <% @doses.each do |dose| %>
       <li><%= Ingredient.find(dose.ingredient_id).name %> , <%= dose.description %> <%= link_to "Delete", destroy_dose_path(dose), method: :delete %> </li>
      <% end %>
  </li>
</ul>

<%= link_to "+ Add more ingredients", new_dose_path(@cocktail) %>

</div>
